apiVersion: v1
kind: Service
metadata:
  name: {{ include "taco-cloud-argocd.fullname" . }}
  labels:
    {{- include "taco-cloud-argocd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taco-cloud-argocd.selectorLabels" . | nindent 4 }}
