apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "taco-cloud-argocd.fullname" . }}-test-connection"
  labels:
    {{- include "taco-cloud-argocd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "taco-cloud-argocd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
